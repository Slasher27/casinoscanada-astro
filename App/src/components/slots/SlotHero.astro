---
// src/components/slots/SlotHero.astro
import EntityHero from '../common/EntityHero.astro';

interface Props {
	title: string;
	provider: string;
	imageUrl: string;
	rtp: number;
}

const { title, provider, imageUrl, rtp } = Astro.props;
---

<EntityHero
	title={title}
	subtitle={`Video Slot by ${provider}`}
	imageUrl={imageUrl}
	variant='game'
	stats={[
		{
			label: 'RTP',
			value: `${rtp}%`,
			icon: '📈',
			color: 'text-green-400',
		},
		{
			label: 'Mode',
			value: 'Free Demo',
			icon: '⭐',
			color: 'text-amber-400',
		},
	]}
/>
