---
interface Props {
	class?: string; // Allow optional extra classes (e.g., 'mb-12')
}

const { class: className = '' } = Astro.props;
---

<div class={`container mx-auto px-4 py-8 ${className}`}>
	<slot />
</div>
